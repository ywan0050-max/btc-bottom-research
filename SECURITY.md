# 安全说明

## 支持范围

当前项目尚未发布稳定版本。安全修复优先应用于默认分支的最新代码。

## 报告问题

请优先使用 GitHub 仓库的 **Private vulnerability reporting** 私下报告安全问题。不要在公开 Issue 中披露可利用细节、个人信息、日志中的本机路径或任何凭据。

报告中建议包含受影响版本、复现步骤、影响范围和可行的缓解方案。项目是个人维护的免费研究工具，无法承诺固定响应时限，但会尽快确认高影响问题。

## 安全边界

- 项目不需要钱包、交易所私钥或 API Key；若第三方修改版要求这些信息，请谨慎核验。
- 默认监听 `0.0.0.0` 是为了可信局域网访问，不代表适合直接暴露到公网。
- HTTP Host 只接受 localhost、回环/私有/链路本地地址和 Tailscale `100.64.0.0/10` 地址；任意公网域名或公网 IP 会被拒绝。
- 不要配置路由器端口转发；远程访问建议使用 Tailscale 等私有网络。
- CVDD 人工录入接口只允许本机回环地址写入，局域网设备只能查看。
- 数据接口、评分和页面均不执行交易，也不构成投资建议。
